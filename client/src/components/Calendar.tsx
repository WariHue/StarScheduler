import {Schedule} from "@/src/types";
import {StarBlock} from "@/src/components/StarBlock";

export const Calendar: React.FC<{ schedule: Schedule[] }> = ({ schedule }) => {
  let len = new Array(42)

  return (
    <>
      <div className={"grid grid-cols-7"}>
      </div>
    </>
  )
}